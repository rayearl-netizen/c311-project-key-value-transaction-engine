/*
fn lexical_analyzer(content:String){
    enum Token{
        SET,
        GET,
        DELETE,
        IDENTIFIER(String),
        VALUE(String),
        NEWLINE,
        EOF,
    }
    let a = {"SET"};
    let home = IpAddr::SET::from(a);
    print(a);
}

*/